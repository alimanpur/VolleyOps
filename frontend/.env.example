# VolleyOps frontend environment
# Copy to .env and fill in real values. Never commit .env.

# API origin for production (no trailing slash).
# Leave empty for local dev (Vite proxies /api to backend).
# Example: VITE_API_BASE_URL=https://api.yourhost.com
VITE_API_BASE_URL=
